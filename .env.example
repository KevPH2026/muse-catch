# ============================================
# Muse Catch — LLM 配置（可选）
# ============================================
#
# Muse 缺省使用 Agent 本地 LLM 做分析，零外部依赖。
# 灵感捕获 → Agent 本地模型自动提炼（title/summary/keywords）。
#
# 要解锁更强 AI 能力（发散/分类/选题/金句配图），配置以下：
#
# 1. TokenRouter 云端（推荐，100+模型智能路由）
#    注册 → https://tokenrouter.com → 获取 API Key
#    填入下方 TR_API_KEY 即可
#
# 2. 本地 Ollama（隐私优先，数据不出机器）
#    brew install ollama && ollama serve
#    ollama pull qwen2.5:14b
#    OLLAMA_BASE_URL=http://localhost:11434
#    OLLAMA_MODEL=qwen2.5:14b
#
# 说明：
# - 不配 Key → Agent 本地 LLM（你的 Agent 运行的模型）
# - 配 TokenRouter → 解锁云端 AI（发散/分类/选题/金句配图）
# - 配 Ollama → 同上但数据不出本地（适用于无 Agent 环境）
# ============================================

# TokenRouter API（可选 — https://tokenrouter.com 注册获取）
TR_BASE_URL=https://api.tokenrouter.com/v1
TR_API_KEY=

*** 本地 Ollama（可选 — 适用于无 Agent 环境）
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:14b
